--- src/ui/LaunchApp.tsx
+++ src/ui/LaunchApp.tsx
@@ -324,6 +324,11 @@
   });
 
+  const onResize = () => {
+    app.update(prev => ({ ...prev }));
+  };
+  process.stdout.on('resize', onResize);
+
   app.view((state: LaunchState) => {
     const termCols = process.stdout.columns ?? 120;
     const termRows = process.stdout.rows ?? 40;
@@ -599,13 +604,11 @@
       {
         border: 'none',
         width: 'full',
-        height: veryCompact ? undefined : 'full',
+        height: 'full',
         style: { bg: bg(c.bg) },
         px: 0,
         py: compactLayout ? 0 : 1,
       },
       [
-        veryCompact 
-          ? ui.column({ width: 'full', items: 'center', gap: 1 }, [topBlock, bottomBlock])
-          : ui.column({ width: 'full', height: 'full', justify: 'between', items: 'center' }, [topBlock, bottomBlock]),
+        ui.column({ width: 'full', height: 'full', items: 'center' }, [topBlock, ui.spacer({ flex: 1 }), bottomBlock]),
       ],
     );
@@ -620,6 +623,7 @@
     unmount: async () => {
       if (unmounted) return;
       unmounted = true;
+      process.stdout.removeListener('resize', onResize);
       cleanupFocus();
       try {
         await ready;
